# RBC Community Map — runtime dependencies
# Install with: pip install -r requirements.txt

# Qt GUI + embedded browser (QtWebEngineCore/Widgets, QtWebChannel, QtNetwork
# all ship in the standard PySide6 wheel)
PySide6>=6.5

# HTTP requests to the RBC API / game site
requests>=2.28

# HTML parsing
beautifulsoup4>=4.11
